# 2026-03-31 日志

## 11:40 🛡️ 安全审计报告

**触发**: Cron 任务自动执行

**发现**:
- 🔴 严重问题 4 个 - 文件权限过宽
  - `.openclaw\` 状态目录 (mode=666)
  - `openclaw.json` 配置文件 (mode=666)
  - `credentials\` 凭证目录 (mode=666)
  - `auth-profiles.json` 认证配置 (mode=666)
- 🟡 警告 3 个
  - 缺少可信代理配置
  - 浏览器令牌存储在配置中
  - 会话文件权限过宽
- ✅ 攻击面控制良好 (开放组 0，白名单组 1)

**修复建议**:
```powershell
# 收紧权限（管理员运行）
icacls "D:\OpenClaw\.openclaw" /inheritance:r /grant:r "%USERNAME%:F"
icacls "D:\OpenClaw\.openclaw\openclaw.json" /inheritance:r /grant:r "%USERNAME%:F"
icacls "D:\OpenClaw\.openclaw\credentials" /inheritance:r /grant:r "%USERNAME%:F"
icacls "D:\OpenClaw\.openclaw\agents\main\agent\auth-profiles.json" /inheritance:r /grant:r "%USERNAME%:F"
icacls "D:\OpenClaw\.openclaw\agents\main\sessions\sessions.json" /inheritance:r /grant:r "%USERNAME%:F"
```

**状态**: 报告已发送到 Telegram (messageId: 4220)

---

## 11:43 🛡️ 安全审计报告 (第 2 次)

**触发**: Cron 任务自动执行

**结果**: 与上次相同，未发现新问题

**状态**: 报告已发送到 Telegram (messageId: 4222)