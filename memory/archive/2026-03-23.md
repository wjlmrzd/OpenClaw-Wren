## 2026-03-23: Telegram 图片接收问题诊断进展

### 问题
- Telegram 文本消息正常接收 ✅
- Telegram 图片消息完全无法接收 ❌

### 日志分析结果
检查 `D:\tmp\clawdbot\clawdbot-2026-03-23.log` 发现：
- 有文本消息的 `telegram raw-update` 日志记录
- **完全没有**图片消息的任何日志记录
- 结论：图片消息在 Telegram Bot API 层面就没有被推送到 OpenClaw

### 根本原因定位
问题在 `allowed_updates` 配置：
- 代码位置：`C:\Users\Administrator\AppData\Roaming\npm\node_modules\openclaw-cn\dist\telegram\allowed-updates.js`
- 当前逻辑：使用 grammy 的 `API_CONSTANTS.DEFAULT_UPDATE_TYPES` + `message_reaction`
- 可能问题：grammy 默认值可能不完整，或 webhook 注册时未正确应用

### 已执行操作
1. 启用 debug logging: `"logging": { "level": "debug" }`
2. 重启 Gateway 以重新注册 webhook
3. 等待用户发送测试图片验证

### 待验证
- Gateway 重启后发送图片，检查日志是否有 `photo` 字段
- 如仍无日志，需手动检查 webhook 配置或修改 `allowed_updates` 返回值

### 关键文件
- 日志：`D:\tmp\clawdbot\clawdbot-2026-03-23.log`
- 配置：`D:\OpenClaw\.openclaw\openclaw.json`
- 代码：`dist/telegram/allowed-updates.js`, `dist/telegram/webhook.js`
