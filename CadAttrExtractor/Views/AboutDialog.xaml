<Window x:Class="CadAttrExtractor.Views.AboutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="关于 - 图纸目录提取器"
        Height="320" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource PanelBackground}">

    <Window.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="30,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PrimaryDarkBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Logo and Title -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center">
            <!-- Using a text-based logo since we don't have an actual image -->
            <Border Background="{DynamicResource PrimaryBrush}"
                    CornerRadius="8"
                    Width="64" Height="64"
                    Margin="0,0,0,16">
                <TextBlock Text="CTE"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>

            <TextBlock Text="图纸目录提取器"
                       FontSize="{DynamicResource HeaderFontSize}"
                       FontWeight="Bold"
                       Foreground="{DynamicResource PrimaryText}"
                       HorizontalAlignment="Center"/>

            <TextBlock x:Name="VersionTextBlock"
                       Text="版本 1.0.0"
                       FontSize="{DynamicResource SmallFontSize}"
                       Foreground="{DynamicResource SecondaryText}"
                       HorizontalAlignment="Center"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Description -->
        <StackPanel Grid.Row="1" Margin="0,24,0,0">
            <TextBlock Text="AutoCAD 插件，用于从图块属性中提取数据，"
                       Foreground="{DynamicResource SecondaryText}"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="支持坐标排序、表格生成和 Excel/Word 导出。"
                       Foreground="{DynamicResource SecondaryText}"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"
                       Margin="0,4,0,0"/>

            <Border Height="1"
                    Background="{DynamicResource BorderBrush}"
                    Margin="0,20"/>

            <TextBlock Text="功能特性:"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource PrimaryText}"
                       Margin="0,8,0,4"/>
            <TextBlock Text="• 智能属性提取"
                       Foreground="{DynamicResource SecondaryText}"/>
            <TextBlock Text="• 四轴坐标排序"
                       Foreground="{DynamicResource SecondaryText}"/>
            <TextBlock Text="• DWG 表格生成"
                       Foreground="{DynamicResource SecondaryText}"/>
            <TextBlock Text="• Excel/Word 模板导出"
                       Foreground="{DynamicResource SecondaryText}"/>
        </StackPanel>

        <!-- Footer -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="© 2024 WrenTools. 保留所有权利。"
                       FontSize="{DynamicResource SmallFontSize}"
                       Foreground="{DynamicResource SecondaryText}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,16"/>

            <Button Content="确定"
                    Style="{StaticResource ButtonStyle}"
                    HorizontalAlignment="Center"
                    Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
